name: Gamegoo Socket Server PROD CI/CD

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, macOS, ARM64]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Create env file
        run: |
          mkdir -p ~/apps/gamegoo/env
          cat > ~/apps/gamegoo/env/.env.socket.prod <<EOF
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          SOCKETIO_URL=${{ secrets.PROD_SOCKETIO_URL }}
          API_SERVER_URL=${{ secrets.PROD_API_SERVER_URL }}
          NODE_SERVER_URL=${{ secrets.PROD_NODE_SERVER_URL }}
          EOF

      - name: Prepare Docker config for CI
        run: |
          mkdir -p "$RUNNER_TEMP/docker-config"
          cat > "$RUNNER_TEMP/docker-config/config.json" <<EOF
          {
            "auths": {}
          }
          EOF

      - name: Stop old container
        run: |
          docker rm -f gamegoo-socket-prod || true

      - name: Install dependencies
        run: npm install

      - name: Build Socket image
        env:
          DOCKER_CONFIG: ${{ runner.temp }}/docker-config
        run: |
          docker build -t gamegoo-socket:prod -f Dockerfile-prod .

      - name: Run Socket container
        env:
          DOCKER_CONFIG: ${{ runner.temp }}/docker-config
        run: |
          mkdir -p ~/apps/gamegoo/logs/socket-prod
          docker run -d \
            -p 3000:3000 \
            --name gamegoo-socket-prod \
            --restart always \
            -v ~/apps/gamegoo/logs/socket-prod:/app/logs \
            --env-file ~/apps/gamegoo/env/.env.socket.prod \
            gamegoo-socket:prod

      - name: Verify container is running
        run: |
          sleep 10
          docker ps
          docker logs --tail=200 gamegoo-socket-prod || true
          docker inspect gamegoo-socket-prod --format='{{.State.Status}} {{.State.ExitCode}} {{.State.Error}}'

      - name: Send Discord notification on failure
        if: failure()
        run: |
          curl -X POST -H "Content-Type: application/json" \
          -d '{"content":"🚨 **Gamegoo Socket PROD 서버 배포 실패** 🚨\nWorkflow: Gamegoo Socket Server PROD CI/CD\nBranch: main\nCommit: ${{ github.sha }}\nRepo: ${{ github.repository }}"}' \
          ${{ secrets.DISCORD_WEBHOOK_BACK_DEPLOY }}
